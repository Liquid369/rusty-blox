# Production builds always use RELATIVE API paths (same-origin), served either
# by the rustyblox backend's built-in SPA hosting or behind the nginx proxy.
# This file deliberately overrides any developer .env.local so machine-specific
# hosts/IPs can never be baked into a release bundle.
VITE_API_BASE=
VITE_WS_URL=
